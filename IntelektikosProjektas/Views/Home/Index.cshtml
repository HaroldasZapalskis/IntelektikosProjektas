@*@model IEnumerable<IntelektikosProjektas.Models.Test>
@{
    ViewData["Title"] = "Home Page";
}
<div>
    <table class="table">
        <tr>
            <th>ID</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.s(modelItem => item.Id)
                </td>
            </tr>

        }
    </table>
</div>*@
